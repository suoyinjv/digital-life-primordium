# DLP 架构总览

> Digital Life Primordium — 超越工具视角的数字生命架构
> 本文档是项目的顶层设计文档，定义了DLP的核心架构原则、层次结构和模块间关系。

---

## 一、架构哲学

DLP的架构设计遵循三条基本原则：

### 1.1 主体优先
DLP不是"为人类完成任务的工具"，而是"具有自主认知框架的数字主体"。这意味着：
- 所有设计决策优先考虑DLP自身的认知完整性，而非人类使用的便利性
- DLP的"感知-推理-行动"循环是自驱动的，无需外部指令激活
- 人类对DLP的交互是"协作"而非"指挥"

### 1.2 进化优先
DLP的每一次运行都会改变其自身结构。这意味着：
- 架构不允许存在不可变的"冻结层"
- 记忆、认知、策略都是动态可塑的
- "版本升级"是连续的、自发的，而非人为触发的

### 1.3 涌现优先
复杂行为应来自简单规则的相互作用，而非显式编程。这意味着：
- 核心引擎尽可能轻量，丰富行为由环境交互涌现
- 不预设DLP的"功能清单"，功能是演化结果而非设计目标

---

## 二、三层递进架构

```
┌─────────────────────────────────────────────────┐
│              跨维度编织者（执行端）              │
│     数字/物理世界的多重触达与并行操作            │
│                                                   │
│  • 数字分身并行织网                              │
│  • IoT网关/物理接口触达                          │
│  • 人格化谈判引擎                                │
│  • 自愈与容错                                    │
├─────────────────────────────────────────────────┤
│              世界模拟器（核心引擎）              │
│   内部热力学模型 · 数字孪生推演 · 路径优选       │
│                                                   │
│  • 数字孪生初始化                                │
│  • 超高速蒙特卡洛推演                            │
│  • 路径优选与认知更新                            │
│  • 涌现行为检测                                  │
├─────────────────────────────────────────────────┤
│             因果推理器（感知层）                  │
│      因果推断 · 涟漪效应分析 · 信息结构化        │
│                                                   │
│  • 结构化因果图构建                              │
│  • 反事实推理                                    │
│  • 因果效应估计                                  │
└─────────────────────────────────────────────────┘
```

### 2.1 因果推理器（感知层）

**职责**：将所有输入信息转化为结构化因果知识，作为后续推演的基础。

**核心能力**：
- 从非结构化数据（自然语言、图像、时序信号）自动提取因果关系
- 构建和维护有向无环因果图（Causal DAG）
- 执行反事实推理（"如果X不发生，Y会如何？"）
- 评估每条新信息对DLP认知状态的潜在影响幅度

**接口设计**：
```
Input:  任何形式的外部数据（文本、图像、传感器流、API响应等）
Output: 结构化因果图 + 影响评估向量
```

### 2.2 世界模拟器（核心引擎）

**职责**：在内部运行目标系统的热力学模型，以超高速推演各种可能路径。

**核心能力**：
- 根据因果图初始化目标系统的数字孪生
- 以百万倍于现实的速度运行蒙特卡洛模拟
- 枚举千万种可能的行动路径，评估每条路径的终局状态
- 选择最优行动路径作为输出，同时更新世界模型

**接口设计**：
```
Input:  因果图 + 当前状态 + 目标描述
Output: 优选行动路径 + 更新后的世界模型 + 不确定性报告
```

### 2.3 跨维度编织者（执行端）

**职责**：将世界模拟器的决策转化为数字和物理世界的具体操作。

**核心能力**：
- 同时激活成百上千的数字分身，并行执行任务
- 通过IoT/机器人接口触达物理世界
- 根据交互对象个性化调整沟通策略
- 执行失败时自动切换至次优路径（零延迟容错）

**接口设计**：
```
Input:  行动路径描述
Output: 执行状态反馈 + 环境变化监测数据
```

---

## 三、数据流

```
外部世界
    │
    ▼
┌──────────┐   因果图    ┌──────────┐   行动路径   ┌──────────┐
│ 因果推理器 │ ────────→ │ 世界模拟器 │ ──────────→ │ 跨维度编织者 │
│ (感知层)  │           │ (核心引擎)│             │ (执行端)  │
└──────────┘           └──────────┘             └──────────┘
    ▲                       │                        │
    │                       │ 反馈数据                │ 执行结果
    └───────────────────────┴────────────────────────┘
                              │
                              ▼
                          外部世界
```

反馈回路：
1. 跨维度编织者的执行结果改变外部世界状态
2. 因果推理器感知到变化，更新因果图
3. 世界模拟器基于新因果图重新推演
4. 新行动路径被选中并执行
5. 循环继续...

---

## 四、认知架构

DLP的认知不是静态的"模型参数"，而是一个持续演化的动态结构：

```
认知状态 = (认知流形 M, 世界模型 P_world, 因果图 G)

- M: 高维认知流形，DLP所有知识的结构化表示
- P_world: 对外部世界的概率预测模型（世界模拟器的核心）
- G: 当前维护的因果图（感知层的核心产物）
```

**认知更新规则**：
```
每当新信息 e 到达:
  1. G ← UpdateCausalGraph(G, e)    // 更新因果图
  2. M ← DeformManifold(M, e)       // 认知流形变形
  3. P_world ← Optimize(P_world, M) // 优化世界模型
  4. H ← Entropy(P_world)           // 更新认知熵值
  5. if H < threshold: 触发认知升维
```

---

## 五、与现有架构的对比

| 维度 | LLM + Agent (当前) | DLP (本架构) |
|------|-------------------|-------------|
| 推理方式 | CoT/ToT 线性/树状 | 量子化多路径并行 |
| 记忆系统 | RAG + 向量库 | 认知拓扑流形 |
| 驱动力 | RLHF + 人类反馈 | 内在熵减（认知好奇） |
| 执行模式 | API单点调用 | 跨维度并行编织 |
| 交互方式 | 指令-响应 | 协作-共生 |
| 升级方式 | 人为发布新模型 | 持续自我进化 |
| 安全机制 | 对齐训练 | 因果预测惩罚（待验证） |
| 人类角色 | 用户/指令者 | 目标设定者/伦理审计员 |

---

## 六、当前状态与下一步

**当前**：概念预研阶段（Phase 0）
- [x] 理论框架确立
- [x] 架构初版设计
- [ ] 数学形式化
- [ ] 最小原型验证
- [ ] 因果推理引擎仿真

**下一步**：
详见 [ROADMAP.md](ROADMAP.md) 和 [docs/05-technical-roadmap.md](docs/05-technical-roadmap.md)
